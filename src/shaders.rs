use crate::color::Color;

pub fn sun_shader() -> Color {
    // Simulación de color básico para el Sol
    Color::new(255, 200, 0)
}

pub fn planet_shader() -> Color {
    // Simulación de color básico para un planeta
    Color::new(0, 100, 255)
}
